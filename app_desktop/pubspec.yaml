name: photo_sync_desktop
description: 照片同步 - 电脑端桌面应用
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  photo_sync_core:
    path: ../packages/photo_sync_core
  provider: ^6.1.1
  qr_flutter: ^4.1.0
  flutter_local_notifications: ^17.0.0
  window_manager: ^0.3.8
  path_provider: ^2.1.2
  shared_preferences: ^2.2.2
  path: ^1.8.3
  uuid: ^4.2.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
